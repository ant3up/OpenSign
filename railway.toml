[build]
builder = "nixpacks"

[deploy]
startCommand = "npx serve -s apps/OpenSign/dist -l tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.environment]
NODE_ENV = "production"
